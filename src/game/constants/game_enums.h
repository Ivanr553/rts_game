#pragma once

typedef enum faction
{
    _FACTION,
    FACTION_ROBOT,
    FACTION_ALCHEMISTS
} FACTION;

typedef enum unit_class
{
    _UNIT_CLASS,
    WORKER,
    GROUND_COMBAT,
    FLYING_COMBAT
} UNIT_CLASS;

typedef enum resource
{
    _RESOURCE,
    RESOURCE_CRYSTAL
} RESOURCE;